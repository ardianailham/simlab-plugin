name: Plugin Compatibility

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      # Remove the "Install Composer dependencies" step
      - name: Run Tests
        run: |
          ./vendor/bin/phpunit
